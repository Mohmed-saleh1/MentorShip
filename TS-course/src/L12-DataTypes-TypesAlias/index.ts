/**
 * Data Types
 * ..Type Alias => we can combine more than data type together 
 */

type st = string;
let theName :st="Muhamed"

type num = number;
 let theNumber : num = 20;

 type stAndNum = string|number // => the same with  type stAndNum = st|num
 let alll:stAndNum="ahmed"
  alll= 9;